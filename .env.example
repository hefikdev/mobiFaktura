# ===================================
# mobiFaktura Environment Configuration
# ===================================
# Note: Copy this file to .env and adjust values for your environment
# Never commit .env to version control!

# -----------------------------------
# Environment Mode
# -----------------------------------
# Options: development, production
NODE_ENV=development

# -----------------------------------
# Application URL
# -----------------------------------
# Public URL where the application is accessible
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------
# Database Configuration
# -----------------------------------
# For local development (outside Docker): use localhost
# For Docker Compose: use 'postgres' as hostname
DATABASE_URL=postgres://mobifaktura:mobifaktura_secret@localhost:5432/mobifaktura

# PostgreSQL credentials (used by Docker)
POSTGRES_USER=mobifaktura
POSTGRES_PASSWORD=mobifaktura_secret
POSTGRES_DB=mobifaktura

# -----------------------------------
# MinIO S3 Storage Configuration
# -----------------------------------
# For local development (outside Docker): use localhost
# For Docker Compose: use 'minio' as hostname
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=mobifaktura_minio
MINIO_SECRET_KEY=mobifaktura_minio_secret
MINIO_BUCKET=invoices
MINIO_USE_SSL=false

# -----------------------------------
# Authentication & Security
# -----------------------------------
# JWT secret key (minimum 32 characters) - CHANGE IN PRODUCTION!
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-long-here

# Session cookie name
SESSION_COOKIE_NAME=mobifaktura_session

# -----------------------------------
# Emergency Super Admin (CRITICAL!)
# -----------------------------------
# Used for emergency access when all admin accounts are locked/lost
# Access via /login - system validates credentials against these values
# SECURITY: Store these credentials securely, never in version control!
SUPER_ADMIN_EMAIL=emergency@yourdomain.com
SUPER_ADMIN_PASSWORD=YourSecurePassword123!

# -----------------------------------
# Backup Configuration (Optional)
# -----------------------------------
# Cron schedule for automatic backups (format: minute hour day month weekday)
# Default: 0 2 * * * means 2 AM daily
BACKUP_CRON_SCHEDULE=0 2 * * *

# Number of days to retain backups before deletion
BACKUP_RETENTION_DAYS=30

# -----------------------------------
# Logging Configuration
# -----------------------------------
# Log level: debug, info, warn, error
# Use 'info' for production, 'debug' for development
LOG_LEVEL=info

# ===================================
# Docker Instructions
# ===================================
# When running with Docker Compose:
# 1. Change DATABASE_URL hostname from 'localhost' to 'postgres'
# 2. Change MINIO_ENDPOINT from 'localhost' to 'minio'
# 3. Update NEXT_PUBLIC_APP_URL to your domain
# 4. Change NODE_ENV to 'production'
# 5. Generate strong random values for JWT_SECRET
# 6. Set up proper SUPER_ADMIN credentials
# ===================================
